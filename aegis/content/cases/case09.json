{
  "id": "case09",
  "title": "Firewall Gap",
  "track": "blue",
  "cert_objective": "CySA+ CS0-003 Domain 2 — Vulnerability Management",
  "xp_base": 150,
  "difficulty": 3,
  "tools_type": "rule_analyzer",
  "challenge_data": "DENY ANY 0.0.0.0/0 203.0.113.47 22\\nALLOW ANY 0.0.0.0/0 203.0.113.47 443\\nDENY ANY 0.0.0.0/0 203.0.113.47 3306\\nALLOW ANY 0.0.0.0/0 ANY ANY|||203.0.113.1 203.0.113.47 8080 INBOUND\\n203.0.113.1 203.0.113.47 22 INBOUND\\n10.0.0.99 185.220.101.45 4444 OUTBOUND\\n203.0.113.44 203.0.113.47 443 INBOUND",
  "scenario": "AEGIS TERMINAL v1.0 — VERIDIAN SYSTEMS SOC\n\nINCOMING TICKET — SECURITY ENGINEERING\nPRIORITY: HIGH\nANALYST: YOU\n\nRoot cause analysis: how did the attacker reach nginx on port 8080?\nThe firewall team has provided the ruleset active during the attack.\n\nFIREWALL RULES (evaluated top-down, first match wins):\n  1. DENY  ANY → 203.0.113.47:22   (block SSH)\n  2. ALLOW ANY → 203.0.113.47:443  (allow HTTPS)\n  3. DENY  ANY → 203.0.113.47:3306 (block MySQL)\n  4. ALLOW ANY → ANY:ANY           (allow everything else)\n\nTRAFFIC DURING ATTACK WINDOW:\n  203.0.113.1  → 203.0.113.47:8080  (attacker → nginx)\n  203.0.113.1  → 203.0.113.47:22    (attacker → SSH)\n  10.0.0.99    → 185.220.101.45:4444 (C2 beacon outbound)\n  203.0.113.44 → 203.0.113.47:443   (legitimate HTTPS)\n\nWhat port should have a DENY rule to prevent the initial compromise?",
  "challenge": "Which specific port number should have had an explicit DENY rule in this ruleset to block the attacker's initial access?",
  "valid_answers": ["8080"],
  "hints": [
    "Go to https://www.cloudflare.com/learning/network-layer/what-is-a-firewall/\nRead about firewall rule ordering — 'first match wins'. A packet\nis allowed or denied by the first rule that matches it.\nLook at what rule matched the attacker's connection to port 8080.",
    "Trace the attacker's connection (203.0.113.1 → 203.0.113.47:8080)\nthrough the rules top-down. Rule 1 is for port 22 — no match.\nRule 2 is for port 443 — no match. Rule 3 is for port 3306 — no match.\nRule 4 is ALLOW ANY → ANY — match. Port 8080 falls through to the\ncatch-all ALLOW. Type 'tools' to run the rule analyzer.",
    "Type 'tools' in the game. The rule analyzer evaluates each traffic\nentry against the rules and shows which rule matched. Look for the\nattacker's connection and which port has no specific DENY rule.",
    "SPOILER — Port 8080 had no explicit DENY rule. The catch-all\nALLOW rule (rule 4) permitted the attacker to reach nginx.\nThe fix: add 'DENY ANY 0.0.0.0/0 203.0.113.47 8080' before rule 4.\nType: 8080"
  ],
  "steps": [
    {
      "question": "Trace the attacker's connection (203.0.113.1 → 203.0.113.47:8080) through the firewall rules top-down. Which rule number matches this connection first?",
      "valid_answers": ["rule 4", "4", "rule 4 allow any"],
      "hints": [
        "Firewall rules are evaluated top-down — the first matching rule applies (first match wins)",
        "Rule 1: DENY port 22 — does this match port 8080? No. Move to Rule 2.",
        "Rule 2: ALLOW port 443 — does this match port 8080? No. Rule 3: DENY port 3306 — does this match? No.",
        "Rule 4 is 'ALLOW ANY → ANY:ANY' — this catch-all matches everything not caught by rules 1-3, including port 8080."
      ],
      "wrong_answer_hints": [
        {"pattern": "rule 1", "response": "Rule 1 is DENY port 22 (SSH). The attacker connected to port 8080 — different port, no match."},
        {"pattern": "rule 2", "response": "Rule 2 is ALLOW port 443 (HTTPS). Port 8080 doesn't match port 443."}
      ]
    },
    {
      "question": "The catch-all ALLOW rule (Rule 4) permitted the attacker's connection to port 8080. What specific port number should have had an explicit DENY rule to block this access?",
      "valid_answers": ["8080"],
      "hints": [
        "The attacker reached nginx on port 8080 — that port had no explicit DENY rule",
        "Ports 22, 443, and 3306 all have explicit rules. Which port used by nginx is missing a DENY rule?",
        "The catch-all Rule 4 caught port 8080 because there was no specific rule for it before Rule 4",
        "Port 8080 should have an explicit DENY rule added before the catch-all ALLOW rule. Type: 8080"
      ],
      "wrong_answer_hints": [
        {"pattern": "4444", "response": "Port 4444 is also a gap (the C2 beacon went out), but the initial compromise used port 8080. That's the primary gap."},
        {"pattern": "22", "response": "Port 22 already has an explicit DENY rule (Rule 1). The missing rule is for a different port."}
      ]
    },
    {
      "question": "To fix the firewall gap, where in the ruleset (before which rule number) should the new DENY rule for port 8080 be inserted?",
      "valid_answers": ["before rule 4", "before 4", "rule 4", "above rule 4"],
      "hints": [
        "Since rules are evaluated top-down, a new DENY must appear before the catch-all ALLOW rule",
        "The catch-all ALLOW rule is Rule 4. The new DENY must be inserted somewhere before Rule 4.",
        "The new DENY rule should be added as Rule 4 (pushing the current Rule 4 to Rule 5) or before any rule that would catch port 8080 first",
        "The fix: add 'DENY ANY 0.0.0.0/0 203.0.113.47 8080' before the catch-all ALLOW rule (before Rule 4)."
      ],
      "wrong_answer_hints": []
    }
  ],
  "learn": "Firewall rule analysis is a core vulnerability management skill. Misconfigurations\nin firewall rulesets are one of the most common causes of preventable breaches.\n\nKey concepts:\n  First match wins  — rules are evaluated top-down; first matching rule applies\n  Implicit deny     — some firewalls deny all traffic not explicitly permitted\n  Catch-all ALLOW   — 'ALLOW ANY ANY' at the end defeats implicit deny\n  Principle of least privilege — only permit what is explicitly required\n\nCommon firewall misconfigurations:\n  Catch-all ALLOW rules     — as seen here: port 8080 not explicitly blocked\n  Overly broad source ranges — ALLOW from 0.0.0.0/0 instead of specific IPs\n  Missing egress rules       — only inbound rules, no outbound (C2 beacon went out)\n  Rule ordering errors       — ALLOW before DENY for the same traffic\n\nFirewall review checklist:\n  1. Is there an explicit DENY for all unused ports?\n  2. Is the catch-all rule DENY rather than ALLOW?\n  3. Are management ports (22, 3389) restricted to known admin IPs?\n  4. Are egress rules in place for outbound C2 prevention?",
  "tools": "Evaluates each traffic entry against the firewall rules top-down and shows which rule matched and the resulting action.",
  "debrief": {
    "summary": "You identified port 8080 as the firewall gap. The catch-all ALLOW rule permitted all traffic not explicitly denied — and port 8080 was never explicitly denied. The fix is to add a specific DENY rule for port 8080 before the catch-all. The egress gap (port 4444 C2 beacon allowed out) is a bonus finding that would have contained the compromise earlier.",
    "real_world": "Firewall rule reviews are a standard part of vulnerability management programs. Security teams periodically audit rulesets to identify catch-all ALLOWs, unused open ports, and missing egress controls. Many compliance frameworks (PCI DSS, ISO 27001) require periodic firewall reviews.",
    "next_step": "Practice with real tools:\nTryHackMe: 'Firewalls' room\nhttps://tryhackme.com/room/redteamfirewalls",
    "cert_link": "CySA+ CS0-003 Domain 2 — Vulnerability Management:\n\"Given a scenario, analyze and interpret output from security technologies and identify misconfigurations that create vulnerability exposure.\"",
    "exam_tip": "On the exam, firewall rule questions often ask you to trace a packet through a ruleset and identify the matching rule. Remember: first match wins, rules are evaluated top-down. Also know: implicit deny vs explicit deny, and that egress filtering (blocking outbound C2) is just as important as ingress filtering."
  }
}
